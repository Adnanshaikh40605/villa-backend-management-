# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,.railway.app

# Database Configuration
# For local development (SQLite - default if DATABASE_URL not set)
# For Railway/Production (PostgreSQL - set this in Railway environment variables)
# Railway automatically provides DATABASE_URL when you add PostgreSQL service
# Format: postgresql://user:password@host:port/database
# Example: postgresql://postgres:password@postgres.railway.internal:5432/railway
DATABASE_URL=

# JWT Settings (in minutes)
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=10080

# CORS Settings (comma-separated, no spaces)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173
